# Defense in depth: deny all direct web access to this folder even if the
# root .htaccess rewrite rules are ever bypassed or unsupported by the host.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
